import { StyleSheet } from "react-native";

export default StyleSheet.create({
    tabSection: {
        justifyContent: 'center', 
        alignItems: 'center',
        flex: 1,
        // flexShrink: 1
    }
})